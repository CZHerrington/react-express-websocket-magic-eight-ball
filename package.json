{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.15.2",
    "react-notification-system": "^0.3.0",
    "socket.io": "^1.7.3",
    "styled-components": "^4.4.1"
  },
  "scripts": {
    "start": "node index.js",
    "build": "npm install; npm run app:build",
    "app:build": "cd app; npm install; npm run build",
    "dev": "cd app; npm start"
  }
}
