{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.15.2",
    "socket.io": "^1.7.3"
  },
  "scripts": {
    "start": "node index.js",
    "app:build": "cd app; npm install; npm run build",
    "app:dev": "cd app; npm start"
  }
}